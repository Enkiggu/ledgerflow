global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'ledgerflow-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['ledgerflow-app:8080']
        labels:
          application: 'ledgerflow'
          environment: 'production'
